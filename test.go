package main

import (
	"fmt"
	"strings"
)

func main() {
	// fmt.Println("Hello, World")
	// array := [5]int{10, 20, 30, 40, 50}

	// var arrayOne [5]int
	// arrayTwo := [5]*int{0: new(int), 1: new(int)}
	// *arrayTwo[0] = 10
	// *arrayTwo[1] = 20
	// fmt.Println(array)
	// arrayOne[0] = 10
	// fmt.Println(arrayOne)
	// fmt.Println(arrayTwo)
	// var arrayThree [1e6]int
	// foo(&arrayThree)
	// slice := []int{1, 2, 3}
	// slice = append(slice, 4)
	// fmt.Println(slice)
	// slice := make([]int, 2, 2)
	// slice[0] = 1
	// slice[1] = 2
	// slice = append(slice, 3)
	// fmt.Println(slice[0])
	// slice := [][]int{{10}, {100, 200}, {50, 150}}
	// slice[0] = append(slice[0], 20)
	// slice = append(slice, 3)
	// fmt.Println(slice)
	// slice := make([]int, 1e6)
	// slice = foo(slice)
	// fmt.Println(slice)
	// colors := map[string]string{}
	// colors["Red"] = "red"
	// fmt.Println(colors)
	// _, exists := colors["Red"]
	// if exists {
	// 	fmt.Println(exists)
	// fmt.Println(value)

	// fmt.Println(5)
	// x := "my new text is this long"
	// y := strings.Repeat("#", len(x))
	// fmt.Print(y)
	// fmt.Println("Liben")
}

// }

// func foo(slice []int) []int {
// 	return slice
// }

// func foo(a *[1e6]int) {
// 	fmt.Println(a)
// }
